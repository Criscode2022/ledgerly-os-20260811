services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-me}
      DATABASE_URL: ${DATABASE_URL:-}
    restart: unless-stopped
